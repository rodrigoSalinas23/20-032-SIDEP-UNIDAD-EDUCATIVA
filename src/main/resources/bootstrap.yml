spring:
  application:
    name: api-sidep-unidadeducativa
  cloud:
    config:
      failFast: true
      uri: http://dockerdesa.mineduc.cl/properties-centralizados/
      server:
        native:
          searchLocations: classpath:/
